# Настройка GitHub Secrets

## 📋 Текущие секреты

### Backend репозиторий
У вас уже есть все необходимые секреты:
- ✅ `CLOUDFLARE_ORIGIN_CERT`
- ✅ `CLOUDFLARE_ORIGIN_KEY`
- ✅ `DATABASE_URL`
- ✅ `DOMAIN`
- ✅ `JWT_SECRET`
- ✅ `OVERLAY_URL`
- ✅ `RATE_LIMIT_WHITELIST_IPS` (см. ниже)
- ✅ `TWITCH_CALLBACK_URL`
- ✅ `TWITCH_CLIENT_ID`
- ✅ `TWITCH_CLIENT_SECRET`
- ✅ `TWITCH_EVENTSUB_SECRET`
- ✅ `VPS_HOST`
- ✅ `VPS_PORT`
- ✅ `VPS_SSH_KEY`
- ✅ `VPS_USER`
- ✅ `WEB_URL`

### Frontend репозиторий
У вас уже есть все необходимые секреты:
- ✅ `CLOUDFLARE_ORIGIN_CERT`
- ✅ `CLOUDFLARE_ORIGIN_KEY`
- ✅ `VITE_API_URL`
- ✅ `VPS_HOST`
- ✅ `VPS_PORT`
- ✅ `VPS_SSH_KEY`
- ✅ `VPS_USER`

## 🔧 Рекомендуемые дополнительные секреты

### ✅ RATE_LIMIT_WHITELIST_IPS (Backend)

**Название:** `RATE_LIMIT_WHITELIST_IPS`

**Описание:** Список IP-адресов (через запятую), которые должны быть исключены из rate limiting. Эти IP-адреса не будут ограничены по количеству запросов, но все запросы будут логироваться для мониторинга.

**Формат:** `95.165.98.28` или `95.165.98.28,192.168.1.1` (для нескольких IP)

**Как добавить:**
1. Перейдите в Settings → Secrets and variables → Actions
2. Нажмите "New repository secret" (или обновите существующий)
3. Name: `RATE_LIMIT_WHITELIST_IPS`
4. Value: `95.165.98.28` (или список IP через запятую)
5. Нажмите "Add secret"

**Важно:**
- IP-адреса из whitelist не будут ограничены rate limiting
- Все запросы с whitelisted IP будут логироваться с пометкой `[RATE_LIMIT] WHITELIST`
- Это позволяет отслеживать активность без блокировки запросов
- При следующем деплое через CI/CD секрет автоматически добавится в `.env` файл

### ⚠️ Важно: DATABASE_URL_BETA (Backend)

**Для полной изоляции beta от production рекомендуется добавить:**

**Название:** `DATABASE_URL_BETA`

**Описание:** URL подключения к отдельной базе данных для beta окружения

**Формат:** `postgresql://user:password@host:port/memalerts_beta?schema=public`

**⚠️ ВАЖНО:** Убедитесь, что в конце указано `schema=public` (не `schema=publi` или другое значение)!

**Важно:**
- Если секрет не добавлен, beta будет использовать ту же базу данных, что и production (`DATABASE_URL`)
- Это может привести к тому, что тестирование в beta повлияет на production данные
- **Рекомендуется создать отдельную базу данных для beta**

**Как создать отдельную базу данных:**

1. Подключитесь к PostgreSQL на вашем VPS:
   ```bash
   sudo -u postgres psql
   ```

2. Создайте новую базу данных:
   ```sql
   CREATE DATABASE memalerts_beta;
   ```

3. Создайте пользователя (если нужно) или используйте существующего:
   ```sql
   -- Если используете существующего пользователя, пропустите этот шаг
   -- Замените 'your_password' на реальный пароль
   CREATE USER memalerts_user WITH PASSWORD 'your_password';
   GRANT ALL PRIVILEGES ON DATABASE memalerts_beta TO memalerts_user;
   ```

4. Выйдите из psql:
   ```sql
   \q
   ```

5. Добавьте секрет в GitHub:
   - Перейдите в Settings → Secrets and variables → Actions
   - Нажмите "New repository secret"
   - Name: `DATABASE_URL_BETA`
   - Value: `postgresql://user:password@host:port/memalerts_beta?schema=public`
   - **Убедитесь, что указано `schema=public` (не `publi`!)**

6. Миграции применятся автоматически при следующем деплое из ветки `develop`

**Проверка секрета:**
После добавления секрета убедитесь, что:
- ✅ В конце URL указано `schema=public` (не `publi`)
- ✅ Имя базы данных: `memalerts_beta` (или другое, но должно отличаться от production)
- ✅ Пользователь и пароль корректны
- ✅ Хост и порт указаны правильно

## ✅ Проверка настройки

После добавления секретов:

1. **Backend:**
   - При деплое из ветки `develop` будет использоваться `DATABASE_URL_BETA` (если добавлен) или `DATABASE_URL` (fallback)
   - Beta backend будет работать на порту `3002`
   - Beta backend будет использовать домен `beta.{DOMAIN}`

2. **Frontend:**
   - При деплое из ветки `develop` будет использоваться относительный URL для API (автоматически подхватит beta API)
   - Beta frontend будет доступен на `beta.{DOMAIN}`

## 🔒 Безопасность

- ✅ Beta и production полностью изолированы (отдельные базы данных, порты, домены)
- ✅ CORS настроен так, что beta backend принимает только запросы с beta frontend
- ✅ Cookies изолированы по доменам (beta cookies не доступны в production и наоборот)
- ✅ Beta доступ контролируется через `hasBetaAccess` флаг в базе данных

## 📝 Примечания

- Если вы не хотите создавать отдельную базу данных для beta, можете не добавлять `DATABASE_URL_BETA` - система будет использовать `DATABASE_URL` с fallback
- Однако это не рекомендуется для production окружения, так как тестирование может повлиять на production данные
- Все остальные секреты уже настроены и готовы к использованию

